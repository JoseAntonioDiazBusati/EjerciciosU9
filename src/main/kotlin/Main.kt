package org.example
import org.example.data.dao.UsuariosDAOH2
import org.example.data.db.Database
import org.example.datos.dao.LineaPedidoDAO
import org.example.datos.dao.PedidoDAO
import org.example.datos.dao.ProductoDAO
import org.example.model.LineaPedido
import org.example.model.Pedido
import org.example.model.Producto
import org.example.model.Usuario
import org.example.service.LineaPedidoService
import org.example.service.PedidoService
import org.example.service.UsuarioService
import org.example.service.ProductoService

fun main() {
/**
 * ejercicio2
    val usuario1 = Usuario( "Facundo Pérez",  "facuper@mail.com")
    val usuario2 = Usuario( "Ataulfo Rodríguez",  "ataurod@mail.com")
    val usuario3 = Usuario( "Cornelio Ramírez",  "Cornram@mail.com")

    val usuariodao = UsuariosDAOH2()
    val usuarioservicio = UsuarioService(usuariodao)

    usuarioservicio.insert(usuario1)
    usuarioservicio.insert(usuario2)
    usuarioservicio.insert(usuario3)
    println(usuarioservicio.getAll())
    *************************
    val producto1 = Producto("Ventilador", 10.0,2)
    val producto2 = Producto("Abanico", 150.0,47)
    val producto3 = Producto("Estufa", 24.99,1)

    val productoDAO = ProductoDAO()
    val productoService =  ProductoService(productoDAO)

    productoService.insert(producto1)
    productoService.insert(producto2)
    productoService.insert(producto3)

    println(productoService.getAll())
    *************************
    val pedido1 = Pedido(160.0,2)
    val pedido2 = Pedido(20.0,1)
    val pedido3 = Pedido(150.0,2)

    val pedidodao = PedidoDAO()
    val pedidoService = PedidoService(pedidodao)

    pedidoService.insert(pedido1)
    pedidoService.insert(pedido2)
    pedidoService.insert(pedido3)
    **************************
    val lineaPedido1 = LineaPedido(1,10.0,1,1)
    val lineaPedido2 = LineaPedido(1,150.0,1,2)
    val lineaPedido3 = LineaPedido(2,20.0,2,1)
    val lineaPedido4 = LineaPedido(1,150.0,3,2)

    val lineapedidodao = LineaPedidoDAO()
    val lineaPedidoService = LineaPedidoService(lineapedidodao)

    lineaPedidoService.insert(lineaPedido1)
    lineaPedidoService.insert(lineaPedido2)
    lineaPedidoService.insert(lineaPedido3)
    lineaPedidoService.insert(lineaPedido4)*/



}